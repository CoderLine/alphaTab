Title: EnablePlayer
JsName: player
DataAttribute: data-player
Category: Player
Description: Gets or sets whether the player should be enabled.
ShowInSideBar: false
---

<h2>Description</h2>
<p>
    This setting configures whether the player feature is enabled or not. Depending on the platform enabling the player needs some additional actions of the developer. 
    For the JavaScript version the <a href="@Context.GetLink("/reference/property/soundfontfile.sf2")">player</a> property must be set to the URL of the sound font that should be used. 
    For .net manually the sound font must be loaded. 
</p>

<h2>Types</h2>

<table class="table table-striped table-condensed type-table">
    <tbody>    
        <tr>
            <td><code>bool <span class="code-badge">.net</span></code></td>
        </tr>   
        <tr>
            <td><code>string <span class="code-badge">JavaScript and HTML</span></code></td>
        </tr>   
    </tbody>
</table>

<h2>Default Value</h2>

<code>true</code>

<h2>Example - C#</h2>

<pre>
<code class="language-csharp line-numbers">
var settings = Settings.Defaults;
settings.EnablePlayer = true;
</code>
</pre>

<h2>Example - JavaScript</h2>

<pre>
<code class="language-javascript line-numbers">
var settings = {
    player: "/assets/soundfont.sf2"
};
var api = new alphaTab.platform.javaScript.AlphaTabApi(document.querySelector('#alphaTab'), settings);
var japi = $('#alphaTab').alphaTab(settings);
</code>
</pre>

<h2>Example - HTML</h2>

<pre>
<code class="language-html line-numbers">
&lt;div id="alphaTab" data-player="/assets/soundfont.sf2"&gt;&lt;/div&gt;
</code>
</pre>