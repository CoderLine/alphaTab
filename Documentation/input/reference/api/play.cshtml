Title: Play()
JsName: play()
jQueryName: alphaTab('play')
Category: Methods - Player
Description: Starts the playback of the current song.
ShowInSideBar: false
---

<h2>Description</h2>
<p>
     Starts the playback of the current song if possible.Reasons for not starting can be that the player is not ready yet or it is already playing.
</p>

<h2>Signatures</h2>

<table class="table table-striped table-condensed type-table">
    <tbody>    
        <tr>
            <td><code>void Play()<span class="code-badge">.net</span></code></td>
        </tr>   
        <tr>
            <td><code>function play() <span class="code-badge">JavaScript</span></code></td>
        </tr>   
        <tr>
            <td><code>alphaTab('play) <span class="code-badge">jQuery</span></code></td>
        </tr>   
    </tbody>
</table>

<h3>Parameters</h3>
None

<h3>Returns</h3>
true if the playback was started, otherwise false. Reasons for not starting can be that the player is not ready or already playing.

<h2>Example - C#</h2>

<pre>
<code class="language-csharp line-numbers">
var api = new AlphaTabApi<MyControl>(...);
api.Play();
</code>
</pre>


<h2>Example - JavaScript</h2>

<pre>
<code class="language-javascript line-numbers">
var api = new alphaTab.platform.javaScript.AlphaTabApi(document.querySelector('#alphaTab'));
api.play();
</code>
</pre>


<h2>Example - jQuery</h2>

<pre>
<code class="language-javascript line-numbers">
$('#alphaTab').alphaTab('play');
</code>
</pre>