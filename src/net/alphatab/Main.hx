package net.alphatab;
import net.alphatab.model.GsSong;
import net.alphatab.tablature.model.GsSongFactoryImpl;
import net.alphatab.tablature.Tablature;

/**
 * ...
 * @author Daniel Kuschny
 */

class Main 
{
	static function main() 
	{
		/*
		try
		{
				var mc:flash.display.MovieClip = flash.Lib.current;
			var rootStage = mc.parent;
			var tablature:Tablature = new Tablature(mc);

			var demoFile = "\\title 'Smoke on the Water' " + 
				"\\subtitle 'famous riff' " +
				". " +
				"(0.3 0.4).4 (3.3 3.4).4 (5.3 5.4).4  r.8 (0.3 0.4).8 | " +
				"r.8 (3.3 3.4).8 r.8 (6.3 6.4).8 (5.3 5.4).4 r.4 | " +
				"(0.3 0.4).4 (3.3 3.4).4 (5.3 5.4).4  r.8 (3.3 3.4).8 | " +
				"r.8 (0.3 0.4).8";
			var parser = new net.alphatab.file.alphatab.AlphaTabParser();
			var reader = new net.alphatab.platform.BinaryReader();
			tablature.IsError = false;
			reader.initialize(demoFile);
			parser.Init(reader, new GsSongFactoryImpl());
			var song:GsSong = parser.ReadSong();
			tablature.SetTrack(song.Tracks[0]);

		}
		catch(e:Dynamic) 
		{
			trace(e);
		}*/
		
	}

}